[package]
name = "mockaco-diff"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Framework-independent split diff editor model for Mockaco"

[dependencies]
mockaco-core = { path = "../mockaco-core" }
similar = "3.2.0"

[lib]
path = "src/lib.rs"
